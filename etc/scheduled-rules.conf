<rules>
<rule>
importance   = 1
condition    = var(scheduled_event) eq "r&r_sleep"
# stroke_color = dblue
fill_color   = lgrey
# glyph        = rectangle
# glyph_size   = 8
</rule>

<rule>
importance   = 2
condition    = var(scheduled_event) =~ "bme211"
# stroke_color = dred
fill_color   = paired-12-qual-2 #lpurple
# glyph        = rectangle
# glyph_size   = 8
</rule>

<rule>
importance   = 3
condition    = var(scheduled_event) =~ "rotation"
# stroke_color = dred
fill_color   = paired-12-qual-4 #lgreen
# glyph        = rectangle
# glyph_size   = 8
</rule>

<rule>
importance   = 4
condition    = var(scheduled_event) =~ "bme215"
# stroke_color = dred
fill_color   = paired-12-qual-6 #lblue
# glyph        = rectangle
# glyph_size   = 8
</rule>

<rule>
importance   = 5
condition    = var(scheduled_event) =~ "pgms"
# stroke_color = dred
fill_color   = paired-12-qual-8 #lred
# glyph        = rectangle
# glyph_size   = 8
</rule>

<rule>
importance   = 6
condition    = var(scheduled_event) =~ "cmpe"
# stroke_color = dred
fill_color   = paired-12-qual-10 #lyellow
# glyph        = rectangle
# glyph_size   = 8
</rule>

</rules>