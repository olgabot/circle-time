<rules>
<rule>
importance   = 1
condition    = var(activity) eq "r&r_sleep"
# stroke_color = dblue
fill_color   = lgrey
# glyph        = rectangle
# glyph_size   = 8
</rule>

<rule>
importance   = 2
condition    = var(activity) =~ "bme211"
# stroke_color = dred
fill_color   = paired-12-qual-1 #purple
# glyph        = rectangle
# glyph_size   = 8
</rule>

<rule>
importance   = 3
condition    = var(activity) =~ "rotation"
# stroke_color = dred
fill_color   = paired-12-qual-3 #green
# glyph        = rectangle
# glyph_size   = 8
</rule>

<rule>
importance   = 4
condition    = var(activity) =~ "bme215"
# stroke_color = dred
fill_color   = paired-12-qual-5 #blue
# glyph        = rectangle
# glyph_size   = 8
</rule>

<rule>
importance   = 5
condition    = var(activity) =~ "pgms"
# stroke_color = dred
fill_color   = paired-12-qual-7 #red
# glyph        = rectangle
# glyph_size   = 8
</rule>

<rule>
importance   = 6
condition    = var(activity) =~ "cmpe"
# stroke_color = dred
fill_color   = paired-12-qual-9 #yellow
# glyph        = rectangle
# glyph_size   = 8
</rule>

<rule>
importance   = 7
condition    = var(activity) =~ "pdp"
# stroke_color = dred
fill_color   = paired-12-qual-11 #orange
# glyph        = rectangle
# glyph_size   = 8
</rule>

<rule>
importance   = 8
condition    = var(activity) eq "circos"
# stroke_color = dblue
fill_color   = paired-12-qual-11
# glyph        = rectangle
# glyph_size   = 8
</rule>

</rules>